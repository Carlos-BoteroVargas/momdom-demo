{
  "name": "momdom-demo",
  "version": "1.0.0",
  "type": "module",
  "description": "MomDom Demo — Netlify + MongoDB Atlas",
  "scripts": {
    "dev": "npx netlify dev"
  },
  "dependencies": {
    "mongodb": "^6.3.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
